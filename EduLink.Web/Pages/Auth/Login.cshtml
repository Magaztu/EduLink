@page
@{
    ViewData["Title"] = "EduLink – Conecta tu aprendizaje";
}

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>EduLink – Iniciar sesión</title>

    <!-- Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
        href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap"
        rel="stylesheet" />

    <!-- Bootstrap 5 (el que ya trae tu proyecto) -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />

    <!-- Tema EduLink -->
    <link rel="stylesheet" href="~/css/edulink-theme.css" asp-append-version="true" />
</head>
<body class="edulink-auth-body">

    <div class="edulink-auth-wrapper d-flex align-items-center justify-content-center min-vh-100">
        <div class="card edulink-auth-card">
            <div class="card-body p-4 p-md-5">
                <!-- Marca / encabezado -->
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <div class="d-flex align-items-center gap-2">
                        <div class="edulink-logo-circle">
                            <!-- bombilla simple -->
                            <span>💡</span>
                        </div>
                        <div class="edulink-brand">
                            <span class="edulink-brand-main">Edu</span><span class="edulink-brand-highlight">Link</span>
                        </div>
                    </div>

                    <span class="px-3 py-1 edulink-badge">
                        Plataforma educativa
                    </span>
                </div>

                <p class="text-muted small mb-4">
                    
                </p>

                <h1 class="h4 fw-semibold mb-2">Iniciar sesión</h1>
                <p class="text-muted mb-4">
                    Escribe tu correo y contraseña para acceder a tu cuenta.
                </p>

                <form method="post" novalidate>
                    <div class="mb-3">
                        <label for="Email" class="form-label">Correo electrónico</label>
                        <input id="Email"
                               name="Email"
                               type="email"
                               class="form-control"
                               placeholder="tucorreo@ejemplo.com"
                               autocomplete="email"
                               required />
                    </div>

                    <div class="mb-3">
                        <label for="Password" class="form-label">Contraseña</label>
                        <input id="Password"
                               name="Password"
                               type="password"
                               class="form-control"
                               placeholder="••••••••"
                               autocomplete="current-password"
                               required />
                    </div>

                    <div class="d-flex justify-content-between align-items-center mb-3 edulink-remember-row">
                        <div class="form-check m-0">
                            <input class="form-check-input" type="checkbox" id="RememberMe" name="RememberMe" />
                            <label class="form-check-label" for="RememberMe">
                                Recuérdame
                            </label>
                        </div>

                        <a href="#" class="edulink-link-secondary text-decoration-none">
                            ¿Olvidaste tu contraseña?
                        </a>
                    </div>

                    <button type="submit" class="btn btn-primary w-100 mb-3">
                        Iniciar sesión
                    </button>
                </form>

                <div class="text-center small text-muted mt-2">
                    ¿No tienes una cuenta?
                    <a asp-page="/Auth/Register" class="edulink-link-primary text-decoration-none">
                        Crear cuenta
                    </a>
                </div>
            </div>
        </div>
    </div>

    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
